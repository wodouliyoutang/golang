package main

import (
	"fmt"
	_ "time"
)

func main() {
	/*
		a := time.Now()
		fmt.Println(a.Format(time.ANSIC))
		fmt.Println(time.Now().Format(time.ANSIC))

		a := []string {"a","b","c"}
		for k,v :=range a {
			fmt.Println(k,"=>",v)
		}
	*/
	fmt.Println("hello")
}
